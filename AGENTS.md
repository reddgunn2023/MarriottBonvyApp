# AGENTS.md

## Cursor Cloud specific instructions

- The current repository checkout is documentation-only: `README.md` describes the intended MarriottBonvyApp ReactJS product, but no `package.json`, lockfile, source tree, Docker/devcontainer config, Makefile, tests, hooks, or service definitions are committed.
- No local services are required or startable from the committed files yet. Once application source and dependency manifests are added, use the scripts committed with those files as the source of truth for lint/test/build/dev-run commands.
- The Cursor Cloud startup dependency refresh is intentionally a no-op until dependency manifests are committed.
